<nav class="border-b border-border bg-card">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="flex h-16 items-center justify-between">
      <div class="flex items-center space-x-8">
        <%= render "layouts/application_logo" %> 
        <div class="hidden md:flex md:space-x-1">
          <% nav_links = [
            { label: "Dashboard", path: dashboard_path },
            { label: "Passwords", path: "#" },
            { label: "About", path: about_path }
          ] %>
          <% nav_links.each do |link| %>
            <%= link_to link[:label], link[:path], class: "rounded-lg px-3 py-2 text-sm font-medium #{current_page?(link[:path]) ? 'text-foreground bg-accent' : 'text-muted-foreground hover:bg-accent hover:text-accent-foreground'}" %>
          <% end %>
        </div>
      </div>
      <%= render "layouts/user_dropdown" %>
    </div>
  </div>
</nav>
